# Project Instructions

Read `AGENTS.md`, `PROJECT_CHARTER.md`, `ARCHITECTURE.md`, `TESTING.md`, and `AI_WORKFLOW.md` before non-trivial changes.

Use the installed `ai-dev-framework`, Ponytail, `i-have-adhd`, and Graphify skills. Follow selected stack packs and the UI member named in `PROJECT_CHARTER.md`.

If a required skill is unavailable, report the gap instead of pretending it ran. Repair local user setup or populate the repository's `.agents/skills` with the framework installer; do not add agent skills as application dependencies.

Apply Ponytail restraint after tracing the affected flow. Prefer the smallest correct implementation and do not add dependencies, abstractions, or UI systems without a demonstrated need.

For architecture, RCA, migration, onboarding, or cross-component questions, query an existing Graphify graph first or build one when the repository is large enough to justify it. Verify findings against source and follow the Graphify decisions in `AI_WORKFLOW.md`.

Lead responses with the result or next action, number multi-step work, keep tangents out of the active task, and report verification and remaining risk.
