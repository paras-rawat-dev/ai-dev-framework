# Baseline Plan

Build the expense review app with FastAPI and React.

Use a database and make a nice UI. Add upload, totals, flags, and CSV export.

Use tests if needed. Make it performant and clean.

